#!/usr/bin/env node
/**
 * cdps - Cloud provider usage statusline for Claude Code
 *
 * This is a thin wrapper package that re-exports @cdps/core.
 * Install globally: npm install -g cdps
 */

export * from '@cdps/core';
