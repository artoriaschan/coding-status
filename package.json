{
  "name": "cdps-monorepo",
  "version": "0.0.1",
  "description": "CDPS - Cloud provider usage statusline for Claude Code",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "claude-code",
    "statusline",
    "cloud-usage",
    "aliyun",
    "bailian"
  ],
  "license": "MIT"
}
